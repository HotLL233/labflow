/**
 * Shared read-only record cell styling used by the three record views.
 * Keep borders on the table cell itself so every portal has the same grid.
 */
export const recordCellSx = {
  fontSize: '0.8rem',
  lineHeight: 1.45,
  minWidth: 0,
  boxSizing: 'border-box',
  border: '1px solid #d9dfe7',
  borderRadius: '2px',
  bgcolor: '#fff',
  maxHeight: 180,
  overflow: 'auto',
  whiteSpace: 'normal',
  overflowWrap: 'anywhere',
  wordBreak: 'break-word',
  verticalAlign: 'top',
  px: 0.75,
  py: 1,
};
