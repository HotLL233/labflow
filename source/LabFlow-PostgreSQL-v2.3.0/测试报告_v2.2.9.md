# v2.2.9 测试报告

## 已通过

- `cargo check`
- `npm --prefix frontend run typecheck`
- `npm --prefix frontend run build`
- `cargo test --lib api::export_write::tests -- --test-threads=1`（5 项导出单元测试全部通过）
- `cargo build --release`
- Inno Setup 服务器版和热更新包编译

## 未通过（环境前置条件缺失）

- 已执行 `cargo test --lib`：59 项通过、65 项失败、2 项忽略。
- 所有失败项均在启动 PostgreSQL 集成测试时提示 `WORKLOAD_TEST_DATABASE_URL is required for PostgreSQL integration tests`；当前未配置隔离测试数据库，不能将该项标记为通过。
- 本次新增的导出单元测试不依赖数据库，已单独通过。

## 功能验收重点

- RD Sheet 6：仅数量列和送样总量。
- 分析检测 Sheet 6：总工作量、检测工作量、辅助工作量位于前列，辅助工作不再生成动态检测列。
- 辅助工作明细表：按人员展示辅助方法数量、系数、工作量和总量。
- Sheet 1-11 原有导出流程和筛选参数保持兼容。

## 数据模型限制

当前数据库没有辅助工作父检测记录字段。导出使用辅助记录关联方法的当前系数，缺失时回退到 `coefficient_snapshot`；若业务需要严格按父检测方法取系数，应先增加明确的父记录数据模型和迁移，不能通过时间或人员猜测关联。

## 环境限制

PostgreSQL 集成测试必须在隔离数据库执行。发布前应配置 `WORKLOAD_TEST_DATABASE_URL` 后补跑 `cargo test --lib` 并记录结果；在此之前，安装包仅完成构建验证和本次导出单元测试验证，未完成全量集成测试验收。
