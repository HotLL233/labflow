server_port = 8000
db_dir = "data"
database_url = "postgresql://workload_app_beta:CHANGE_ME@127.0.0.1:5432/workload_tool_beta"
db_pool_max_size = 12
db_pool_min_idle = 1
log_level = "info"
runtime_log_enabled = true
runtime_log_max_size_mb = 50
admin_user = "admin"
admin_pass = "CHANGE_ME"
backup_enabled = false
backup_interval_hours = 24
max_backup_count = 10
backup_mode = "full"

# 人员变动通知为独立文件功能，不写入 PostgreSQL。
# 在启动服务的环境变量中配置以下值；不要写入业务数据库。
# PERSONNEL_CHANGE_DATA_DIR = "D:/LabFlow/personnel-change-data"
# PERSONNEL_CHANGE_WEBHOOK_URL = "https://oapi.dingtalk.com/robot/send?access_token=..."
