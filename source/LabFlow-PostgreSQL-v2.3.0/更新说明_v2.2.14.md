# v2.2.14 更新说明

## 导出表公式化

- 所有导出表中的计算字段改为 Excel 公式写入，打开表格后可随输入数据变化自动重算。
- 工作量统一按 `数量 × 系数` 计算，倍率不参与工作量。
- 金额统一按 `数量 × 单价/金额基数 × 倍率` 计算。
- 人员、辅助工作明细及各类汇总使用 `SUM` 公式。
- 仪器汇总的按类型检测量使用公式汇总。
- 检测人明细中相同人员、检测类型、方法和系数的记录合并展示。

## 兼容性

- 基于 v2.2.10 基线隔离迭代，未恢复注册功能或部门角色逻辑。
- 保留数量、系数、单价、倍率等业务输入字段为可编辑数值。
