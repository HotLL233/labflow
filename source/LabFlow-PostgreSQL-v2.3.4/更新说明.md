# LabFlow v2.3.4 更新说明

## 取样后工作量录入

- 研发送样在完成取样后可打开“录入工作量”弹窗，默认带入本次取样数量和方法默认倍率。
- 样品信息登记按项目、实验室和检测类型查询可用仪器；只有一个仪器时自动带入，多个仪器时必须选择后才能确认。
- 工作量记录保存取样来源、来源业务编号和实际仪器信息；同一次取样只能录入一条工作量。
- 已录入工作量的取样不能直接撤回，需先按既有工作量处理流程处理，避免统计与取样状态不一致。
- 新增独立权限：`sample:record-workload` 与 `sample-info:record-workload`。

## 版本与发布

- 新版本源码目录：`source/LabFlow-PostgreSQL-v2.3.4`。
- 完整安装包：`LabFlow-v2.3.4.exe`。
- 热更新包：`LabFlow-v2.3.4-HotUpdate.exe`。
- Docker 镜像：`ghcr.io/hotll233/labflow:2.3.4`。

# LabFlow v2.3.3 更新说明

## 记录页面边框统一

- 样品信息登记、研发送样和分析检测记录统一使用相同的字段边框样式。
- 边框直接应用于记录单元格，统一字号、内边距、自动换行和内容滚动规则。
- 状态、附件和操作列保留专用控件，但与普通字段共享相同的单元格布局。
- 本版本基于 v2.3.2 独立目录迭代，未复制 `target/`、`installer/` 或 `frontend/node_modules/` 构建产物。

## 版本与发布

- 新版本源码目录：`source/LabFlow-PostgreSQL-v2.3.3`。
- 完整安装包：`LabFlow-v2.3.3.exe`。
- 热更新包：`LabFlow-v2.3.3-HotUpdate.exe`。
- Docker 镜像：`ghcr.io/hotll233/labflow:2.3.3`。

# LabFlow v2.3.2 更新说明

## 记录页面显示优化

- 研发送样、分析检测和样品信息登记等记录字段统一增加只读边框样式。
- 长文本默认自动换行，超长内容支持在字段框内上下或左右查看。
- 日期、业务编号、状态、附件和操作列保留各自的专用显示方式。
- 移动端继续支持记录列表横向查看，减少字段被压缩的问题。

## 版本与发布

- 新版本源码目录：`source/LabFlow-PostgreSQL-v2.3.2`。
- 完整安装包：`LabFlow-v2.3.2.exe`。
- 热更新包：`LabFlow-v2.3.2-HotUpdate.exe`。
- Docker 镜像：`ghcr.io/hotll233/labflow:2.3.2`。
- Docker 发布工作流支持配置 Gitee Packages Registry，并同步推送版本号和 `latest` 标签。
- Docker 部署文件记录 Gitee 源码仓库地址，并通过拉取脚本按 `LABFLOW_IMAGE_PRIORITY` 优先尝试 Gitee 镜像，失败后回退 GHCR。
- Gitee 镜像路径由 `LABFLOW_GITEE_IMAGE` 配置；实际 Registry 地址需使用 Gitee Packages 页面提供的地址。

# LabFlow v2.3.1 更新说明

## 公告管理

- 公告发布管理改为记录列表，支持新增、编辑、删除、启用和停用。
- 公告支持普通、提醒、重要三个级别及优先级设置。
- 公告可分别投放到首页、研发送样、分析检测和样品信息登记入口。
- 用户端公告弹窗及入口展示沿用系统现有界面风格。

## 发布说明

- 完整安装包：`LabFlow-v2.3.1.exe`。
- 热更新包：`LabFlow-v2.3.1-HotUpdate.exe`。
- Docker 镜像：`ghcr.io/hotll233/labflow:2.3.1`。



# LabFlow v2.2.19 更新说明

## 全量备份改进

- 全量 ZIP 备份新增附件预览目录 `attachment_previews/`。
- 新增人员变动数据目录 `personnel-change-data/`。
- 备份清单升级为 format version 3，记录来源目录、文件数量、总字节数和 SHA-256。
- 大文件改用流式写入 ZIP，降低备份过程的内存占用。
- 恢复流程支持上述新增目录，并继续兼容 v2.2.18 的旧 ZIP。
- 旧备份缺失的新目录时不会清空当前预览或人员变动数据。

## Docker 改进

- 预览和人员变动目录固定在 `/app/data` 持久化卷内。
- 应用镜像安装 PostgreSQL 18 客户端，支持页面执行数据库备份和恢复。
- Docker 发布工作流继续只构建 `linux/amd64`，避免 ARM 模拟构建超时。
- 恢复上传接口允许最大 2 GiB，避免大于 2 MiB 的备份被框架默认限制拒绝。

## 升级注意事项

从 Windows 迁移时，请将原 `ProgramData\\LabFlow\\pv` 目录内容导入备份的 `attachment_previews/`。
配置快照仅用于参考，恢复不会覆盖当前 Docker 的数据库连接、密码、端口和数据目录配置。



# 更新说明 v2.2.16

## 检测类型按实验室和入口显示

- 基于 `v2.2.14` 源码隔离开发，未合并 `v2.2.15` 辅助工作改动。
- 新增检测类型可见性配置，可按分析检测、研发送样、样品信息登记分别设置实验室范围。
- 默认所有启用实验室、所有入口显示全部启用检测类型；仅保存管理员主动关闭的范围。
- 方法和项目查询统一应用实验室/入口检测类型过滤，避免不同录入页面显示不一致。
- 检测类型列表现在按入口、实验室和可用方法/仪器联动返回；无可用方法的类型名称不再显示。
- 分析检测与研发送样提交增加可见性二次校验，防止管理员变更后旧页面提交隐藏类型。
- 历史记录、统计、通知和导出继续读取原检测类型，不因停用或隐藏而改写历史数据。
- 复用现有 `manage:methods` 权限和审计日志，不新增注册、部门角色或旧角色映射。

## 版本隔离

- 源码目录：`LabFlow-PostgreSQL-v2.2.16-detection-type-visibility-source`。
- 基线目录：`LabFlow-PostgreSQL-v2.2.14-all-export-formulas-source`。
- 2.2.14 基线目录未修改；验证生成的构建产物未保留在交付源码目录。

## 验证结果

- `cargo check`：通过。
- `npx tsc --noEmit`：通过。
- 检测类型入口标识单元测试：通过。
- PostgreSQL 集成迁移测试：未执行，当前环境未设置 `WORKLOAD_TEST_DATABASE_URL`。



# v2.2.9 导出统计修复

## 修复内容

- RD 送样统计 Sheet 6 仅显示各检测类型送样数量和送样总量，不再显示系数或工作量。
- 分析检测统计 Sheet 6 将总工作量、检测工作量、辅助工作量置于前列。
- 辅助工作不再占用 Sheet 6 的动态检测类型列，避免表格过宽。
- 新增“辅助工作明细表”，按人员和辅助方法展示数量、系数、工作量及辅助工作总量。
- 辅助工作明细的系数优先使用关联方法当前系数，缺失时回退到工作记录快照。
- 导出接口增加 `sheet12` 选择支持；原有 Sheet 1-11 行为保持不变。

## 数据兼容说明

当前 `work_records` 表没有辅助工作与主检测记录的父子外键，因此本版本不伪造主记录关联，也不新增数据库字段。历史记录继续按现有快照和方法关联读取。

## 版本与发布

- Cargo、前端、锁文件、`VERSION` 和 Inno Setup 脚本统一为 `2.2.9`。
- 服务器版安装包适用于新电脑；热更新包仅适用于已有完整服务器版。
- 发布前应生成 `installer/checksums.sha256`。



# v2.2.4 样品信息登记编辑修复

- 编辑表单的所属部门改为中文名称下拉展示，保存时仍提交对应的部门 ID。
- 未取样记录可由提交账号或送样人对应的实际账号编辑，二者都必须拥有“样品信息登记-编辑本人记录”权限。
- 同一权限范围内，实际送样人账号可查看、上传和删除未取样记录附件。
- 升级时自动补齐历史记录的实际送样人归属；无法匹配系统账号的送样人不会被额外授权。
- 修复前端网络错误提示的乱码。
- 仅修改样品信息登记编辑权限、部门编辑展示和错误提示，不调整其他业务功能。



# v2.2.3 用户导入导出格式兼容

- 用户导出改为生成“研发送样用户导入”和“分析检测用户导入”两个工作表。
- 导出字段与现有用户导入模板一致，角色按“角色1、角色2……”分列。
- 导出文件可直接上传到用户导入功能；已有用户仍需勾选“按所属工作表更新已有用户”。
- 不导出用户密码、启用状态和管理员状态，避免敏感信息泄露及误修改。
- 仅修改用户导出和用户导入兼容逻辑，其他业务功能保持不变。



# v2.2.2 教程连续滚动阅读

- 教程源文档预览改为连续纵向阅读，所有预览页按原顺序上下排列，阅读方式与国标网在线查看一致。
- 阅读区域支持纵向滚动，隐藏横向溢出，避免页面左右翻页。
- 保留目录、教程附件下载、适合页面、适合宽度、缩放和页码定位功能。
- 页码输入仅用于滚动定位到指定页，不改变连续阅读模式。
- 保持普通用户仅查看教程、下载教程附件的权限规则。
- 未修改人员变动通知、通知中心、样品信息登记、工作量及其他既有业务逻辑。
- 安装包随附 PDF、PostgreSQL 和 VC++ 运行时依赖，发布前执行完整性检查。



# 更新说明 v2.2.18

## 导出模板编辑

- 管理中心新增统一的 Excel 式导出模板编辑器。
- 分析检测、研发送样和样品信息登记导出均可配置工作表启停、名称、颜色、列标题、列宽和列显示状态。
- 导出时实际应用保存的模板；统计公式、数据来源与固定统计列顺序继续由系统保护。
- 自动兼容历史模板，只保留仍对应当前导出列的名称、列宽和显示配置。

## 安装

- 完整服务器安装包包含 PostgreSQL 运行时、PDF 预览运行时和 Microsoft Visual C++ x64 运行库。
- 覆盖安装保留现有业务数据、数据库和管理员账号。



# v2.2.14 更新说明

## 导出表公式化

- 所有导出表中的计算字段改为 Excel 公式写入，打开表格后可随输入数据变化自动重算。
- 工作量统一按 `数量 × 系数` 计算，倍率不参与工作量。
- 金额统一按 `数量 × 单价/金额基数 × 倍率` 计算。
- 人员、辅助工作明细及各类汇总使用 `SUM` 公式。
- 仪器汇总的按类型检测量使用公式汇总。
- 检测人明细中相同人员、检测类型、方法和系数的记录合并展示。

## 兼容性

- 基于 v2.2.10 基线隔离迭代，未恢复注册功能或部门角色逻辑。
- 保留数量、系数、单价、倍率等业务输入字段为可编辑数值。



# v2.2.13 更新说明

- 检测人工作量明细按检测人、检测类型、方法和系数合并相同方法，避免同一方法因不同项目拆成多行。
- 同一合并行的多个仪器以去重后的名称合并显示。
- 工作量明细使用 Excel 公式 `=系数×数量`，人员工作量汇总使用 `SUM` 公式。
- 工作量仍不使用倍率；倍率仅用于金额报表。



# v2.2.12 更新说明

## 工作量导出修复

- 固定人员工作量明细、辅助工作明细汇总、人员工作量汇总三张表的字段顺序和标题。
- 工作量统一按 `数量 × 系数`，倍率仅用于金额计算，不参与工作量。
- 升级旧数据库时自动标准化历史导出模板，避免旧列定义和旧标题覆盖新表。
- 保留其它金额统计表的原有模板配置能力。

## 验证范围

- Rust 导出写入层回归测试。
- 前端类型检查和生产构建。
- 实际下载 Excel 后检查三张工作量表结构及公式。



# v2.2.11 工作量、金额与人员变动反馈修复

## 工作量与金额

- 工作量统一为“数量 x 系数”。
- 金额保持“数量 x 单价/金额基数 x 倍率”。
- 倍率不再参与人员工作量、辅助工作量、统计预览和 Excel 工作量导出。
- 人员工作量导出按记录的系数快照统计，历史记录不会因后续修改方法系数而变化。
- 工作量明细列增加浅灰底，金额相关工作表继续单独显示倍率。

## 人员变动反馈

- 人员变动入口保持在“帮助与反馈”。
- 跨实验室人员调动拆分为“跨实验室人员调出”和“跨实验室人员调入”：原实验室组长只迁出，目标实验室组长另行迁入。
- 新员工姓名必填；新增项目必须手工填写至少一个检测方法；新增方法只要求项目和方法名。
- 系统管理员和分析检测组长审批后，通知状态显示“已同步”，并写入项目及人员汇总表。
- 只有系统管理员和分析检测组长可以删除通知；删除已同步通知不会回滚汇总关系，且保留审计记录。
- 普通研发送样组长不能浏览人员关系汇总，只能在授权实验室内查询填报所需的项目和人员关联。

## 兼容性

- 旧配置中的“跨实验室人员调动”会自动按“跨实验室人员调出”处理，并自动补充“跨实验室人员调入”选项。
- 本版本不恢复部门角色、注册功能或旧角色映射。



# v2.2.10 人员工作量汇总与导出修复

## 本版本内容

- 新增分析检测统计 Sheet 13“人员工作量汇总”。
- 采用检测人纵向合并、检测类型逐行显示的表格结构。
- 同一检测类型存在不同系数或倍率时自动拆分为多行，不计算平均系数。
- 工作量按“数量 × 系数 × 倍率”计算，人员总工作量为明细行合计。
- 新增统计页面入口和预览接口，沿用现有统计权限及数据范围。
- 修复研发送样导出 Sheet 5、Sheet 6 使用同名“送样人汇总”导致 Excel 保存失败的问题。
- 自定义工作表名称重复或超过 Excel 31 字符限制时增加唯一名称回退处理。

## 兼容性

- 原有 Sheet 1-12、研发送样统计、样品信息登记及其他业务功能保持不变。
- Sheet 13 仅属于分析检测统计，研发送样统计不会出现该报表。
- 本版本不新增数据库表和字段。



# v2.2.1 教程在线预览优化

- 帮助与反馈先展示教程文档卡片，点击后进入独立查看页。
- 在线查看页增加目录、适合页面、适合宽度、缩放、页码跳转和分页控制。
- 预览区域限制在当前窗口内，避免教程页面撑满整个窗口。
- 保持普通用户仅查看教程、下载教程附件的权限规则。
- 保留 2.2.0 数据、权限和其他业务功能兼容性。



# v2.2.0

## 教程与反馈

- 教程页面以管理员上传的源文档为准，保留 PDF、Word、Excel、图片原文件。
- PDF 和 Office 文件复用样品附件的分页预览能力；转换失败时仍可下载源文件。
- 普通用户仅可查看启用教程和下载教程附件，管理员可上传、替换、显隐和删除。
- 新增 `help_attachments` 教程附件表及对应上传、下载、显隐、删除接口。
- 修复帮助文档和文章 `BIGINT is_visible` 被按布尔值读取导致显隐失效的问题。

## 用户管理

- 新增当前角色模型的用户 Excel 导出，包含角色名称和稳定标识。
- 导入继续兼容旧模板，仅使用功能角色，不再创建或展示部门角色治理映射。

## 移除范围

- 移除公司级权限与数据治理审计、旧角色待映射、历史归属待确认页面及 HTTP 接口。
- 保留业务记录自身的权限范围和历史 ownership 字段，确保现有业务查询不受影响。

## 版本隔离

- 本版本从 v2.1.0 独立复制构建，数据库迁移版本为 `2.2.0-help-tutorial`。



# v2.1.0 更新说明

## 新增与修复

- 工作量录入页新增“项目筛选”下拉框；同一实验室多个项目关联同一检测方法时，可按项目只显示对应的项目-方法录入行。
- 项目筛选与方法类型筛选联动，计数只统计当前项目范围；提交时继续校验方法属于所选项目。
- 通知中心字段候选自动读取录入表单配置（研发送样录入、样品信息录入、分析检测录入）和样品信息登记自定义列。
- 研发送样通知载荷补充 `extra_fields`，录入表单自定义字段可实际渲染到程序内通知和钉钉通知。
- 保留人员反馈字段同步、人员变动通知独立数据边界及 v2.0.0 其他功能。
- 通知中心新增“人员通知反馈驳回”独立通知事件；人员反馈被驳回后立即入队，默认通知反馈发起人，可配置程序内人员和钉钉渠道，并完整显示驳回原因。
- 新增 PostgreSQL 迁移 `2.1.0-personnel-feedback-rejection-notification`，兼容已有通知规则数据。

## 版本隔离

- 本版本基于 v2.0.0 源码复制到独立目录开发。
- 未修改 v2.0.0 源码目录；未改变用户、项目、样品、人员变动通知以外的既有数据库结构。
- 工作量录入项目筛选只改变页面展示条件，不改变录入接口、记录结构和权限校验。

## 验证要求

- `cargo fmt --check`
- `cargo check`
- `cargo test`
- `npm --prefix frontend run typecheck`
- `npm --prefix frontend run build`
- `cargo build --release`
- 完整服务器安装包和热更新包编译检查



# v2.0.0 正式版

- 登录页隐藏“注册”入口，不再在登录页面显示注册提示和注册链接。
- 保留现有登录、用户管理和后端注册接口，未改变其他功能。
- 版本号、前端锁文件、安装脚本和版本隔离标识统一升级为 v2.0.0。



# v1.3.3 通知功能修复

- 人员变动反馈提交后立即处理通知队列，通知不再等待后台 60 秒维护周期。
- 通知规则编辑器将“人员变动反馈”正确显示为中文，并与审批类特殊事件保持互斥。
- 未修改样品信息登记、人员汇总表、人员反馈台账或其他既有业务逻辑。



# v1.3.2 人员反馈与通知修复

- 修复实验室新增项目/新增方法的新增方法输入框输入一个字符后失去焦点的问题。
- 修复已有数据库未升级通知目标约束，钉钉通知规则现在可保存“人员变动反馈”。
- 人员反馈字段支持自定义新增、删除、显示名称、字段类型、适用变动类型、必填、进入通知和启停配置。
- 继续保持人员反馈业务明细使用独立台账，不写入人员反馈业务数据库表；未修改其他业务功能。



# v1.3.1 人员变动反馈优化

## 本版本

- 系统管理员和分析检测组长查看、导出全部实验室；研发送样组长仅查看、导出关联实验室。
- 人员离职、实验室内调动、跨实验室调动默认勾选已有项目，取消勾选即取消关联；员工加入默认不勾选。
- 新增通知中心“人员变动反馈”规则和可配置通知字段，业务明细仍只写独立外部台账及汇总表。
- 管理入口新增“人员反馈字段”，管理员可维护变动类型和字段启停、必填、通知属性。
- 检测方法改为逐条录入，禁止逗号、顿号、分号和换行；汇总表检测方法完整显示并自动换行。

## 隔离与兼容

- 本版本仅使用人员变动功能目录中的 JSON/JSONL 和 Excel 文件，不新增人员反馈业务数据库表。
- 未修改样品信息登记、送样、分析检测、统计、用户和角色等既有业务数据结构。
- 升级安装包保留原 PostgreSQL 数据目录，热更新包备份旧程序和静态资源后再替换。



# v1.3.0 人员变动通知独立版

## 人员变动通知

- 新增独立人员变动通知、近 7 天通知记录和项目及人员汇总表页面。
- 系统管理员可发起、导入导出、查看全部记录和审批；分析检测组长可导入导出、查看全部记录和审批；研发送样组长可发起、查看本人记录和导出，不能审批。
- 通知先进入待审批状态；管理员或分析检测组长通过后才更新独立 Excel 汇总表，并自动备份审批前版本。驳回不会调整汇总表。
- 人员变动模块不写程序 PostgreSQL、旧人员反馈表、站内通知或审计表；台账默认保存到既有应用数据目录下的独立 `personnel-change-data`。
- 实验室内人员调动可选择所选实验室全部项目；跨实验室人员调动已移除目标实验室和目标项目，项目留空即取消当前实验室全部项目关联。
- “新员工加入”更名为“员工加入”；新增项目不再填写项目名称。

## 安装完整性

- 完整服务器安装包会校验 PostgreSQL 关键程序及 Microsoft VC++ x64 运行库。
- 发现旧运行时目录不完整时，安装器会补齐 PostgreSQL 和 VC++ 运行库，避免因缺少 `vcredist_x64.exe` 导致安装退出代码 2。
- 首次安装或环境损坏请使用“PostgreSQL服务器版”完整安装包；热更新包仅用于已有完整服务器的升级。



# 1.2.0-alpha.9 严格角色范围修复测试版

本版本仅修复已知权限边界和版本隔离问题，不改变原有录入、退回、主数据和导出页面流程。

## 修复内容

- 普通角色未配置正式数据范围时不再默认放开全部部门数据。
- 分析检测单条记录与列表统一使用记录有效送样部门判断权限。
- 样品信息登记移除分析检测权限带来的旧宽范围兼容分支。
- 样品信息范围必须绑定部门，类型范围不会单独变成全公司范围。
- 公共账号人员卡片支持基于分析检测模板创建的自定义角色。
- 导出筛选改为事务级配置，避免连接池复用旧筛选条件。



# 样品管理系统 v1.2.0-alpha.8

## 本测试版

- 正式角色的数据来源部门范围成为唯一业务授权来源。
- 旧部门角色不再授予分析检测、研发送样和样品信息登记数据权限。
- 角色管理统一使用“角色模板 + 数据来源部门范围”。
- 旧部门角色仅保留在权限审计和迁移核对中。
- 历史分析记录归属确认后的检测部门、送样部门和实验室快照继续保留。

## 安装包说明

- PostgreSQL 服务器完整安装包：首次安装使用，包含私有 PostgreSQL 运行时和数据库初始化流程。
- 热更新升级包：已有服务器版使用，只替换程序和前端静态资源，不覆盖数据库、配置和业务数据。
- 覆盖安装前建议先完成数据库备份。



# v1.2.0-alpha.4 公司级权限第四测试版

发布日期：2026-08-14

## 本测试版内容

1. 组织治理：部门编码、负责人、启用状态、项目归属部门和协作部门。
2. 权限治理：角色模板、部门角色、按业务域部门范围及部门管理员。
3. 业务归属：分析、研发送样和样品登记保存项目、执行、提交部门及实际业务人员快照；项目关联的实验室必须属于项目归属部门或明确配置的协作部门，编辑记录时同样重新校验。
4. 公共账号：公共账号保留操作审计，实际取样和检测归属到已选择人员。
5. 历史数据：无法可靠判断部门的历史记录标记为待确认，可由管理员确认归属。
6. 统计导出：分析检测、研发送样、样品信息登记支持按部门口径统计和导出；默认不混入历史待确认数据。项目归属统计口径只改变汇总维度，不会突破角色的执行部门数据范围。
7. 审计日志：增加操作账号、实际业务人员和业务部门展示。

## 升级说明

1. 先在测试环境安装或执行热更新，首次启动会自动执行组织治理迁移。
2. 升级后由系统管理员补齐部门编码、负责人、项目归属部门及部门角色。
3. 历史待确认归属记录请在“角色与权限 - 历史归属待确认”中完成确认。
4. 新增或编辑项目时，跨部门执行实验室必须同时完成“项目-实验室关联”和“项目协作部门”配置；仅关联实验室不会自动开放跨部门执行。
5. 本版本为测试版，正式上线前需完成跨部门权限、统计导出和公共账号回归验证。



# v1.2.0-alpha.3 公司级权限第三测试版

## 本版完成

- 新增项目归属部门字段，默认沿用项目主实验室所属部门，也可以在项目管理中明确指定。
- 新增分析检测和研发送样记录的项目归属、执行部门、执行实验室快照。
- 对历史项目和历史记录执行一次性兼容回填。
- 记录列表、单条记录权限、统计、导出和导出预览优先按执行部门快照过滤。
- 新记录、记录编辑、数据治理导入、退回后待修改副本均写入归属快照。
- 保留旧 division_id、group_id 字段作为兼容回退，不改变原有取样、退回、公共账号选人和附件流程。
- PostgreSQL 集成测试配置使用 WORKLOAD_TEST_DATABASE_URL，全量测试通过。

## 数据库迁移

- 1.2.0-alpha.3-record-ownership-snapshots
- 迁移脚本幂等，可在已有 alpha.2 数据库上直接执行。

## 版本隔离

- 本版源码目录：C:\labflow\LabFlow-PostgreSQL-v1.2.0-alpha.3-company-permission-source
- 未复制旧版本构建产物、前端依赖和静态构建目录。
- 热更新包仅替换程序和静态资源，并使用独立的 alpha.3 备份目录。



# v1.2.0-alpha.2 公司级权限第二测试版

## 本版完成

- 用户增加“主归属部门”和“可承担业务部门”两种清晰的部门关系。
- 主归属部门只能选择一个，并且必须包含在可承担业务部门中。
- 可承担业务部门支持多选，用户管理页面和用户接口均使用该范围进行展示与保存。
- 保留 `division_id`、`division_ids` 等旧字段兼容，现有业务流程不改变。
- 旧用户的单部门数据自动迁移为主归属部门和可承担业务部门。
- 用户批量导入模板改为“主归属部门 + 可承担业务部门1-3”，同时兼容旧模板的“所属部门”字段。
- 修复其他公共账号创建时部门字段可能被重新写回的问题。

## 数据库变更

- 新增迁移 `1.2.0-alpha.2-user-primary-and-business-departments`。
- 迁移只整理用户部门关系和 `user_divisions.is_primary`，不修改历史业务记录。

## 测试范围

- 用户新增、编辑、列表展示、部门筛选。
- 用户批量导入和旧模板兼容。
- 分析检测公共账号原有流程保持不变。
- 研发送样、样品信息登记、取样、退回和附件等既有流程未改动。

## 安装说明

- 完整安装包适用于全新安装或覆盖安装。
- 热更新包只替换程序和静态资源，并在目标安装目录 `backup\\v1.2.0-alpha.2` 备份旧程序。
- 本版本构建不复制 alpha.1 的 `target`、`installer` 或其他构建产物。



# v1.2.0-alpha.16 实际场景阻断修复版

## 本版修复

- 实验室新建时明确初始化所属部门，选择部门后可持久化；选择“未分配”可显式清空所属部门。
- 用户管理仅在后端创建成功后显示成功提示；重复用户名等业务失败保留弹窗并显示原因。
- 样品信息登记将部门、实验室、项目初始化请求分别保留结果；页面可区分组织数据加载失败与当前账号无可用部门。
- PostgreSQL 连接池默认下调为 12 个连接、1 个空闲连接，并支持通过 `db_pool_max_size`、`db_pool_min_idle` 配置部署预算。
- 恢复并完成自助注册路由与后端注册入口；注册账号默认不授予业务角色。

## 验收结果

- 隔离 PostgreSQL `max_connections=20` 下，真实页面连续刷新和跨页面操作无“连接数过多”。
- 已完成注册、部门/实验室、用户创建成功与失败、样品登记、附件上传删除、实验室取消部门归属等页面场景回归。
- `cargo test --lib -- --test-threads=1`：116 通过，0 失败，2 忽略。



# v1.2.0-alpha.15 附件兼容与补传修复测试版

## 本版修复

- 兼容旧版本附件：旧 PDF/Word 的存储文件名没有扩展名或扩展名异常时，改为结合原始文件名和 MIME 类型识别，恢复网页预览。
- 对历史预览缓存中“类型暂不支持”的可恢复失败状态自动重新生成，避免旧失败状态永久阻塞预览。
- 样品信息登记记录提交后，在未取样且用户具有本人编辑权限时，允许继续补传和删除附件；分析检测人员和管理员保持原有附件处理权限。
- 前端附件上传、删除按钮与记录状态及当前权限保持一致，避免显示不可执行的操作。
- 修复 PostgreSQL 下送样通知的部门人员筛选参数类型错误；送样事件可再次完成规则匹配、站内通知和钉钉推送。
- 退回待修改记录固定继承原记录的所属部门、实验室和项目，页面显示部门名称；保存时只提交可修改的样品内容，避免组织校验报错和组织信息漂移。

## 本版未改变

- 未改变样品信息登记状态流转、取样、检测、退回和数据范围规则。
- 未改变附件下载、附件保存目录和数据库结构。



# 1.2.0-alpha.14 主数据序号与方法关联修复测试版

## 已完成

- 主数据导出/模板的部门、实验室、检测类型、仪器、检测方法、通用方法、研发项目、项目关联表增加“序号”列。
- 序号仅用于人工定位，不参与导入匹配、更新、删除；旧版没有序号列的文件仍可导入。
- 项目关联继续按数据库中的 `method_id` 建立项目与方法关系，仪器不会被项目再次绑定。
- 导出时项目关联的仪器编号从同一个方法实例生成，避免与检测方法表出现不一致。
- 兼容历史导出文件中“方法名称唯一但仪器编号过期”的情况，回导时自动采用本次方法表中的唯一方法实例。
- 保留 alpha.13 既有样品信息登记入口与 PDF 预览运行时修复。

## 版本隔离

- 本版本只复制源码和运行时依赖，不复制 alpha.13 的 `target`、`installer`、`.dev`、`backend/static`、`frontend/node_modules` 或旧安装包。
- 数据库迁移编号不重复执行历史迁移，升级不修改业务数据和附件原文件。

## 使用说明

- 项目关联中的“关联仪器编号”是同名方法实例的识别字段，不是第二次建立仪器关联。
- 新导出的表会自动填写序号；导入时可以不填写或修改序号。
- 热更新包适用于已有服务器安装，完整安装包适用于新电脑或重新安装运行环境。



# 1.2.0-alpha.13 样品入口与 PDF 预览修复测试版

## 已完成

- 分析检测员和分析检测组长保留样品信息登记入口，可查看其检测部门数据范围内的登记记录。
- 样品信息登记入口与新建权限继续分离，分析检测员不会获得新建登记权限。
- 完整安装包和热更新包均部署 `pdf-runtime`，补齐 `pdftoppm.exe`、`pdfinfo.exe`。
- 打包时校验 PDF 预览组件，组件缺失时直接终止打包。
- 版本与构建产物仅属于 alpha.13，未复制 alpha.12 的 `target`、`installer`、`backend/static`、`frontend/node_modules`。
- 主数据导出/模板的部门、实验室、检测类型、仪器、检测方法、通用方法、研发项目、项目关联表均增加“序号”列；序号仅用于人工定位，不参与导入匹配、更新、删除。
- 项目关联中的“关联仪器编号”继续作为同名方法实例的兼容识别字段，不会在数据库中再次建立项目到仪器的关联；方法与仪器的真实绑定仍只保存在方法记录中。

## 使用说明

- 从旧服务器版升级时运行热更新包即可；数据库、附件原文件和业务数据不变。
- 完整安装包适用于新电脑或需要重新安装服务器运行环境的场景。
- 新模板导入时可以忽略“序号”列；旧版没有序号列的导出文件仍可继续导入。



# 1.2.0-alpha.12 权限与样品登记优化测试版

## 本次修复

- 分析检测员和分析检测组长可进入样品信息登记页面，查看并处理其数据范围内的登记记录。
- 样品信息登记入口与新建、编辑本人记录权限拆分，分析检测员不会看到新建登记表单。
- 增加样品信息登记撤回取样，取样人可将未完成检测的记录恢复为待取样。
- 角色模板补齐样品信息处理权限，并保留角色配置的数据范围约束。
- 修复 PDF 附件预览运行组件缺失；完整安装包和热更新包均会部署 `pdf-runtime`。
- 所有新构建产物只生成在本 alpha.12 源码目录，未复制旧版本构建产物。



# 1.2.0-alpha.11 登录记住与保持登录测试版

## 本次修复

- 登录页将“记住密码”和“保持登录”拆分为两个独立选项。
- “记住密码”启用标准浏览器密码自动填充，并仅保存上次登录用户名及本机偏好；系统不保存明文密码。
- “保持登录”在本设备保存登录令牌，服务端将该会话有效期设为 30 天；未勾选时仍使用原有短时登录会话。
- 主动退出、管理员撤销会话、账号停用或令牌到期后，保持登录令牌会被清除；浏览器已保存的密码仍由手机浏览器自行管理。

## 使用说明

- 私人手机：可同时勾选“记住密码”和“保持登录”。
- 共用设备：不要勾选“保持登录”；如浏览器提示保存密码，请选择不保存。



# 1.2.0-alpha.10 已知问题修复测试版

本版本在 alpha.9 基础上进行隔离迭代，仅修复已确认的权限、公共账号记录归属、导出统计重复、筛选状态残留和版本登记问题，不改变原有页面流程和业务入口。

## 修复内容

- 修正 alpha.10 程序、前端、安装包和热更新包版本标识。
- 修正分析公共账号提交记录的操作人和实际检测人员权限判断。
- 支持基于分析检测模板创建的自定义分析角色。
- 统一分析检测记录的有效数据归属部门判断。
- 修复方法多类型关联造成的导出数量重复风险。
- 将导出筛选配置限制在当前事务，避免连接池复用旧筛选条件。
- 登记 alpha.10 数据库迁移版本，便于安装后核验。
- 新增“代确认并修改已退回的研发送样记录”权限；不自动授予现有角色，且不改变原有数据范围权限。



