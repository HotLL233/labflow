# LabFlow v2.3.24 更新说明

发布日期：2026-09-24

## 一、本次优化：统一记录表物理列模型

### 目标

依据方案 B，以 v2.3.19 的稳定性为基线，保留 v2.3.22 的最小宽度保护，并让表格的列宽、表头、数据行和跨行内容由同一套物理列序列驱动。

### 本次完成

- 创建独立的 `source/LabFlow-PostgreSQL-v2.3.24/` 版本目录。
- 研发送样记录表新增统一 `physicalColumnKeys`，列宽总和、`colgroup`、表头、数据行和空状态统一使用同一列模型。
- 研发送样操作列统一使用 `_action` key，避免 `actions` 与 `_action` 两套结构列名称并存。
- 研发送样表增加 `colgroup`，由最终像素宽度直接定义列轨道，减少表头与内容列宽漂移。
- 样品信息登记记录表统一使用 `physicalColumnKeys` 计算总宽度、`colgroup` 和 `colSpan`。
- 修复样品信息登记编辑行多跨一列的问题。
- 修复历史隐藏列表空状态和展开行多跨两列的问题。
- 保留 `recordTableLayout.ts` 的最小宽度保护、长表头折算、操作列收纳和横向滚动策略。

### 本次暂不处理

- 不改变业务权限、状态流转和操作动作。
- 不新增数据库字段。
- 不迁移统计、日志等非动态记录表页面的 `adaptiveColumns.ts`，避免扩大回归范围。
- 不删除历史隐藏列表代码，先以物理列模型修复并完成构建验证。


## 一、本次优化：基于方案 B 收敛记录表列宽

### 目标

在保留 v2.3.19 稳定布局体验的基础上，继续使用 v2.3.22 的安全列宽引擎，避免列宽塌缩，并降低操作列对数据列的挤压。

### 本次完成

- 创建独立的 `source/LabFlow-PostgreSQL-v2.3.24/` 版本目录。
- 记录表继续使用 `recordTableLayout.ts`，保留容器宽度安全回退、长表头两行折算、最小宽度保护和必要时横向滚动。
- 中等屏幕操作列从 `152px` 收敛为 `124px`，窄屏操作列从 `96px` 调整为 `104px`，为数据列释放空间，同时保留“主操作 + 更多”收纳方式。
- 研发送样记录的布局计算依赖补充 `seq_column_width`，后台序号列宽变化后能够触发布局重新计算。
- 同步更新 `VERSION`、Rust、前端、Cargo 锁文件、Docker Compose 和安装脚本版本到 `2.3.23`。

### 本次暂不处理

- 不改变业务权限、状态流转和操作动作。
- 不新增数据库字段。
- 不修改 `adaptiveColumns.ts` 及录入表单布局。
- 不在本版本同时引入列隐藏优先级、语义列合并或新的列表简称字段。


## 一、本次修复：记录表列宽异常（表头逐字竖排、列被压到 20~30px）

### 现象

研发送样记录与样品信息登记 登记记录中：

- 表头文字逐字竖排。「序号」显示成上下两行的「序」「号」，
  「检测类型」占四行，「注意事项（理化检测样品标注大致含量）」占一整列高；
- 各列被压到 20~30px，一屏只能看到一条记录；
- 「注意事项（…）」这类超长表头列反而比短表头列宽很多，宽度分配明显不合理。

v2.3.21 修好了「容器宽度恒为 0」的问题，但**没有修好本现象**，因为还有两个独立缺陷。

### 根因（两处）

1. **表头按单行宽度参与计算**。后台把 label 配成
   「注意事项（理化检测样品标注大致含量）」（20 字 ≈ 280px），
   引擎就按这个宽度参与分配，被上限截到 240px，独占大量空间；
   而「序号」（2 字）只拿到 56px。宽度分配与可读性脱钩。
2. **压缩没有下限保护**。总量超出容器后，原实现做「整体等比压缩」，
   把自然宽本就等于下限的「序号」一起按比例缩小到 28px 以下，
   于是 2 个字只能逐字换行。

### 修复

- **表头按两行折算**：表头宽度超过 120px 时按 `min(表头宽, 200) / 2` 参与计算，
  单列表头最多按 100px 计入，避免超长表头独占宽度；
- **压缩改为按「可压缩量」比例扣减，且任何列都不低于自身下限**：
  可压缩量 = 目标宽 − 下限，长表头列承担更多压缩，短列保持可读；
- 所有列都到下限仍放不下时，**保持下限并接受横向滚动**，不再压缩到内容不可读；
- 管理员配置为「自定义」的列不参与压缩，尊重既有配置；
- 表头增加 `title` 属性，超长表头可悬停查看完整名称。

### 影响范围

仅前端 3 个文件：

- `frontend/src/utils/recordTableLayout.ts`
- `frontend/src/pages/RdRecordsPage.tsx`
- `frontend/src/components/SampleInfoRecordList.tsx`

后端与数据库无改动；v2.3.20 的迁移不受影响，已升级的库无需重复迁移。

### 使用建议

若某些列的表头仍然过长（例如「检测元素及其精度（铅、镉、砷、汞、铬等）」），
说明该 label 的文字量已超过列宽能承载的范围。建议在后台把这类字段的显示名称改为简短形式
（如「检测元素」「备注」），完整说明可放入「占位提示」或表单内的帮助文字。
后台暂未提供独立的「列表简称」配置项，如需可另行排期。

## 二、验证结果

- 前端 `npm run build`（`tsc` + `vite build`）：通过，产物写入 `backend/static`。
- 后端与 v2.3.21 完全一致（本版未改后端），沿用其
  `cargo check --locked` 与 `cargo build --release --locked` 通过结果。
- `cargo test --locked --lib` 仍因本机未配置 `WORKLOAD_TEST_DATABASE_URL` 无法运行。

## 三、新版本源码目录

`source/LabFlow-PostgreSQL-v2.3.24/`
