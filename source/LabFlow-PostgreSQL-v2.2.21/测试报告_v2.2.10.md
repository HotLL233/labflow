# v2.2.10 测试报告

## 已通过

- `cargo check`
- `cargo test --lib api::export_write::tests -- --test-threads=1 --quiet`
- `npm run typecheck`
- `npm run build`
- Inno Setup 6 已成功编译服务器版和热更新版安装包。
- 已核对服务器版必需运行时文件：`postgres.exe`、`vcredist_x64.exe`、`pdftoppm.exe`、`pdfinfo.exe`。
- 已使用最终安装包重新计算 SHA-256，并写入 `installer/checksums.sha256`。

专项单元测试覆盖：

- 不同系数拆分为独立行。
- 人员总工作量按明细工作量求和。
- 送样人员记录明细与送样汇总表名称不重复。
- Sheet 13 可被导出选择器识别。

## 尚未执行

- 全量 PostgreSQL 集成测试需要配置隔离数据库环境变量 `WORKLOAD_TEST_DATABASE_URL` 后执行 `cargo test --lib`。
- 当前环境未配置该变量，不能将数据库集成测试标记为通过。
- 未执行全新 Windows 电脑上的实机安装启动测试；交付前已完成安装脚本编译、运行时文件存在性检查和安装包哈希校验。
