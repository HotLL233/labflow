# LabFlow v1.2.0-alpha.16 修复回归测试报告

测试日期：2026-08-19  
测试方式：隔离 PostgreSQL、隔离附件目录、真实页面操作；不以接口测试替代场景验收。

## 修复场景

| 场景 | 结果 | 证据 |
|---|---|---|
| 注册页面与注册后跳转登录 | 通过 | `/register` 可打开，注册后进入 `/login` |
| 新建部门 | 通过 | 管理员页面创建部门成功 |
| 新建实验室并绑定部门 | 通过 | 保存、刷新后所属部门仍保留 |
| 实验室取消所属部门 | 通过 | 选择“未分配”后刷新，数据库 `division_id` 为 NULL |
| 用户新建成功提示 | 通过 | 用户出现在列表后才完成成功路径 |
| 重复用户名失败提示 | 通过 | 显示“已存在”，新增用户弹窗保持打开 |
| 样品信息登记部门候选 | 通过 | 已启用部门出现在实际下拉候选中 |
| 样品登记与附件初始上传 | 通过 | 登记成功并带入测试附件 |
| 样品附件删除 | 通过 | 页面提示附件移入回收站 |
| PostgreSQL 连接稳定性 | 通过 | 测试库 `max_connections=20`，连续 10 次跨页面刷新后无连接耗尽；峰值检查时 5 个活动连接 |

## 自动化基线

| 检查项 | 结果 |
|---|---|
| `npm run typecheck` | 通过 |
| `npm run build` | 通过（保留现有大 chunk 警告） |
| `cargo fmt --check` | 通过 |
| `cargo check` | 通过 |
| `WORKLOAD_TEST_DATABASE_URL=... cargo test --lib -- --test-threads=1` | 116 通过，0 失败，2 忽略 |

## 未在本轮实际外部系统执行的项目

- 钉钉 Webhook 实际发送：未配置外部测试通道，不向外部系统发送测试数据。
- 真实备份恢复、安装包覆盖和热更新包安装：避免影响本机安装与数据环境。
- 旧无扩展名 PDF/Word 的真实页面预览、退回待修改完整人工操作链路：已有现有 Rust 覆盖，本次未重复导入历史附件文件。

结论：P1 修复项和本轮补充的 P2 注册路由均已完成并通过隔离环境真实页面回归；上述未执行的外部或高影响场景仍需在发布候选环境按既有流程复验。
